# docker run -it --rm --network stack1_my-net --entrypoint /bin/sh curlimages/curl

services:
  whoami:
    image: traefik/whoami
    command: ["--verbose"]
    labels:
      - "lazy-tcp-proxy.enabled=true"
      - "lazy-tcp-proxy.ports=9000:80"

networks:
  my-net:
    driver: bridge
